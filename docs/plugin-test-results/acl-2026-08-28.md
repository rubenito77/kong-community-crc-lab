# Resultado P03-04: ACL con Key Authentication

## Estado

**Aprobado el 2026-08-28; rollback pendiente.**

## Ejecución

- PipelineRun: `kong-plugin-acl-ntxxc`.
- Estado: `True / Completed`.
- Tasks `clone-repository`, `validate-baseline` y `configure-and-test`:
  `Succeeded`.

## Evidencia funcional

| Caso | Resultado |
|---|---:|
| Línea base sin plugins | HTTP 200 |
| Sin API key | HTTP 401 |
| API key inválida | HTTP 401 |
| Consumer en `acl-allowed` | HTTP 200 |
| Consumer en `acl-denied` | HTTP 403 |
| `apikey` en upstream | Ausente |
| `X-Consumer-Groups` en upstream | Ausente |
| `/demo` | HTTP 200 |
| `/demo2` | HTTP 200 |

## Recursos validados

- `KongPlugin/demo-acl-key-auth`: `hide_credentials=true`.
- `KongPlugin/demo-acl`: permite `acl-allowed` y usa
  `hide_groups_header=true`.
- `KongConsumer/demo-acl-allowed-consumer`: `Programmed=True`.
- `KongConsumer/demo-acl-denied-consumer`: `Programmed=True`.
- Dos Secrets etiquetados `key-auth` y dos Secrets etiquetados `acl`.
- Ingress asociado a `demo-acl-key-auth,demo-acl`.
- PVC `pvc-149e1c9379`: `Bound`, solicitud 100Mi.
- Kong Ingress Controller sin errores de reconciliación.

## Seguridad

Las dos API keys fueron generadas dentro del Pod y compartidas entre Steps
mediante `emptyDir`. No se almacenaron en Git, parámetros, PVC, evidencia o
logs. La inspección de Secrets se limitó a nombre, tipo y etiqueta de credencial.

## Rollback previsto

1. Desasociar ambos plugins del Ingress.
2. Eliminar los dos `KongPlugin`.
3. Eliminar los dos `KongConsumer`.
4. Eliminar los cuatro Secrets.
5. Confirmar `/transform`, `/demo` y `/demo2` en HTTP 200.
6. Confirmar KIC sin errores.

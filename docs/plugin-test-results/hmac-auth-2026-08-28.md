# Resultado P03-05: HMAC Authentication

## Estado

**Aprobado y revertido el 2026-08-28.**

## Ejecución

- PipelineRun: `kong-plugin-hmac-auth-ktkz9`.
- Estado: `True / Completed`.
- Tasks `clone-repository`, `validate-baseline` y `configure-and-test`:
  `Succeeded`.

## Evidencia funcional

| Caso | Resultado |
|---|---:|
| Línea base sin plugin | HTTP 200 |
| Sin firma | HTTP 401 |
| Firma inválida | HTTP 401 |
| Firma válida con fecha vencida | HTTP 401 |
| Firma HMAC-SHA256 válida | HTTP 200 |
| `Authorization` en upstream | Ausente |
| `/demo` | HTTP 200 |
| `/demo2` | HTTP 200 |

La solicitud sin firma devolvió el desafío `WWW-Authenticate: hmac`.

## Recursos validados

- `KongPlugin/demo-hmac-auth`: algoritmo `hmac-sha256`, header `date`,
  `clock_skew=30` y `hide_credentials=true`.
- `KongConsumer/demo-hmac-auth-consumer`: `Programmed=True`.
- `Secret/demo-hmac-auth-credential`: tipo `Opaque`, etiqueta
  `konghq.com/credential=hmac-auth`.
- Ingress asociado a `demo-hmac-auth`.
- PVC `pvc-94b89771ca`: `Bound`, solicitud 100Mi.
- Kong Ingress Controller sin errores de reconciliación.

## Seguridad

Username, secret y firmas fueron generados y utilizados dentro del Pod. No se
almacenaron en Git, parámetros, PVC, evidencia o logs. La inspección del Secret
se limitó a nombre, tipo y etiqueta.

## Rollback completado

- El Ingress quedó desasociado de `demo-hmac-auth`.
- Se eliminaron `KongPlugin/demo-hmac-auth`,
  `KongConsumer/demo-hmac-auth-consumer` y
  `Secret/demo-hmac-auth-credential`.
- `/transform`, `/demo` y `/demo2` respondieron HTTP 200.
- El Ingress conservó `strip-path=true` y quedó sin plugins asociados.
- Kong Ingress Controller no registró errores de reconciliación.

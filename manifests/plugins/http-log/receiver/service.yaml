apiVersion: v1
kind: Service
metadata:
  name: http-log-receiver
  namespace: kong-demo
  labels:
    kong-lab/plugin: http-log
spec:
  type: ClusterIP
  selector:
    app: http-log-receiver
  ports:
    - name: http
      port: 8080
      targetPort: http

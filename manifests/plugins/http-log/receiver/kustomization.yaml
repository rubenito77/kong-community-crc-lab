apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kong-demo
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: http-log-receiver-code
    files:
      - receiver.py
generatorOptions:
  disableNameSuffixHash: true

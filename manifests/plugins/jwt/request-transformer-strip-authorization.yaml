apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: demo-jwt-strip-authorization
  namespace: kong-demo
  labels:
    app.kubernetes.io/part-of: kong-community-crc-lab
    app.kubernetes.io/component: plugin-test
plugin: request-transformer
config:
  remove:
    headers:
      - authorization
